spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/airtickets
    username: postgres
    password: postgres
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update
logging:
  level:
    sql: debug
